{
  "name": "bubblebrowser-workers-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers backend for Bubble Browser web app",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
